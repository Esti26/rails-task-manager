<h1>My tasks</h1>
<% @tasks.each do |task| %>

<li>
  <%= link_to "#{task.title}", show_path(task.id)%>
  <%= link_to "delete", destroy_path(task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>

<% end %>
</li>


<p><%= link_to "Add new task", create_path%></p>
